main: waveletc.o main.o
	g++ waveletc.o -o main.out
	@echo "Rodando testes da classe Wavelet"
	@./main.out

main.o:
	g++ main.cpp -c main.o

wavelet.o:
	g++ waveletc.cpp -c waveletc.o

clean:
	rm *.o *.out
